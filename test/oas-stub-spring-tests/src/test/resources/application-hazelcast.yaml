oas.stub:
  storage:
    type:
      session: hazelcast
      persistent: hazelcast
    hazelcast:
      instance:
        node-ips: "${hazelcast.cluster.members}"
      session-map: session
      persistent-map: persistent
